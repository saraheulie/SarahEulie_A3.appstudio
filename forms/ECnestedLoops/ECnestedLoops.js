row = prompt('Input the number of rows')
column = prompt('Input the number of columns')

for (var i = 0; i <= row; i++) { 
    for (var j = 0; j <= column; j++ ) { 
        document.write('*')
    }
    document.write('<br>')
}